# This is an example environment file.
# For local development, you can copy this to .env and docker-compose will use it.
# In production, these variables should be managed securely (e.g., using infrastructure secrets management).

POSTGRES_USER=user
POSTGRES_PASSWORD=password
POSTGRES_ORDER_DB=order
POSTGRES_PAYMENT_DB=payment
POSTGRES_INVENTORY_DB=inventory